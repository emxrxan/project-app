import { IStackItemStyles } from "@fluentui/react";
import { ThemeColor } from "../ThemeColor";

export const navigationContainerStyles: IStackItemStyles = {
    root: {
        borderBottom: `0.1rem solid ${ThemeColor.GRAY_C8C6C4}`
    }
}